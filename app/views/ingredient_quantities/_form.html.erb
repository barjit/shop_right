<%= form_for(ingredient_quantity) do |f| %>
  <% if ingredient_quantity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ingredient_quantity.errors.count, "error") %> prohibited this ingredient_quantity from being saved:</h2>

      <ul>
      <% ingredient_quantity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :meal_id %>
    <%= f.text_field :meal_id %>
  </div>

  <div class="field">
    <%= f.label :ingredient_id %>
    <%= f.text_field :ingredient_id %>
  </div>

  <div class="field">
    <%= f.label :quantity %>
    <%= f.text_field :quantity %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
